import 'package:flutter/material.dart';

/// Marcador vacío: el shell del paciente muestra el IndexedStack de tabs.
class PatientTabPlaceholder extends StatelessWidget {
  const PatientTabPlaceholder({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
